<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>View Video</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
  <div class="container">
    <h1 class="mt-5"><%= video.title %></h1>
    <a href="/management" class="btn btn-secondary mb-3">Back to Dashboard</a>
    
    <!-- Video Details -->
    <div class="card">
      <img src="<%= video.imageUrl %>" class="card-img-top" alt="<%= video.title %> Artwork">
      <div class="card-body">
        <h5 class="card-title">Author: <%= video.author %></h5>
        <p class="card-text">Year: <%= video.year %></p>
        <a href="<%= video.fileUrl %>" class="btn btn-primary">Watch Video</a>
      </div>
    </div>

    <!-- Form to Edit Video -->
    <form action="/management/video/<%= video._id %>?_method=PUT" method="POST" class="mt-4">
      <h4>Edit Video</h4>
      <div class="mb-3">
        <label for="title" class="form-label">Title</label>
        <input type="text" class="form-control" id="title" name="title" value="<%= video.title %>" required>
      </div>
      <div class="mb-3">
        <label for="author" class="form-label">Author</label>
        <input type="text" class="form-control" id="author" name="author" value="<%= video.author %>" required>
      </div>
      <div class="mb-3">
        <label for="fileUrl" class="form-label">File URL</label>
        <input type="text" class="form-control" id="fileUrl" name="fileUrl" value="<%= video.fileUrl %>" required>
      </div>
      <div class="mb-3">
        <label for="imageUrl" class="form-label">Video Artwork URL</label>
        <input type="text" class="form-control" id="imageUrl" name="imageUrl" value="<%= video.imageUrl %>" required>
      </div>
      <div class="mb-3">
        <label for="year" class="form-label">Year</label>
        <input type="text" class="form-control" id="year" name="year" value="<%= video.year %>" required>
      </div>
      <button type="submit" class="btn btn-success">Update Video</button>
    </form>
  </div>
</body>
</html>
